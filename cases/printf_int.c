#include "../ft_printf.h"

void	printf_dec(obj* flags)
{
	int num;
	char *rep;

	num = va_arg(flags->args, int);
    rep = ft_itao(num);
	if (rep[0] == 0)
    {
        if (rep[1] == x)
    }
}